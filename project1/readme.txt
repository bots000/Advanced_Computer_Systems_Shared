The new file should use threads properly, and there is pretty much nothing of note in the thread function --- I just reassign the data to make sure a new object could be correctly returned.

The .txt file I had was too big to include on github, so you'll have to make one for testing
